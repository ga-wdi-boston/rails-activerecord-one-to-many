<h1>Create a New Album</h1>

<%= form_for @album do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :artist_id %>
  <%= f.collection_select :artist_id, Artist.all, :id, :name, {include_blank: true} %>
  <%= f.submit %>
<% end %>
