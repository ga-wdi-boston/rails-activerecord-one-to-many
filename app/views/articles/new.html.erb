<h1>New article for <%= @user.name %></h1>

<%= form_for [@user, @article] do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :body %>
    <%= form.text_area :body, rows: 10, cols: 80 %>
  </div>
  <%= form.submit %>
<% end %>
